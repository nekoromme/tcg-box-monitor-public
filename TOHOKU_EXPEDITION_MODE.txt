# ============================================================
# 🚗 東北遠征モードのスイッチ
# ============================================================
#
# OFF:
#   通常運転。遠征用の盛岡・仙台監視先は通信も解析もしません。
#
# ON:
#   拠点からWeb応募でき、当選後の店頭訪問が受取1回だけで済む
#   盛岡・仙台方面のうち、遠征用として設定された監視先を一括で有効にします。
#   ※TCバトロコ盛岡大通は常時監視のため、このスイッチの対象外です。
#
# 切り替えるときは、下の OFF を ON に書き換えて保存してください。
# ON / OFF 以外の文字にすると、安全のため監視実行をエラーで止めます。
#
OFF
