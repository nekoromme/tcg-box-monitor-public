name: Regional cashless campaign relay

on:
  schedule:
    # UTC 00:17 / 09:17 = 日本時間 09:17 / 18:17。
    - cron: "17 0,9 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "実行方法（通常はauto）"
        required: true
        default: auto
        type: choice
        options:
          - auto
          - dry-run
          - baseline
          - run

permissions:
  contents: write

concurrency:
  group: regional-cashless-relay
  cancel-in-progress: false

jobs:
  monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONUNBUFFERED: "1"
      RUN_MODE: ${{ github.event_name == 'workflow_dispatch' && inputs.mode || 'auto' }}

    steps:
      - name: 中継リポジトリを取得
        uses: actions/checkout@v4
        with:
          path: .relay-repo
          fetch-depth: 1

      - name: 地域還元モニターの公開コードを取得
        uses: actions/checkout@v4
        with:
          repository: nekoromme/regional-cashless-monitor
          path: app
          fetch-depth: 1

      - name: Pythonを準備
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: app/pyproject.toml

      - name: 必要な部品をインストール
        working-directory: app
        run: python -m pip install --upgrade pip ".[test]"

      - name: 自動テスト
        working-directory: app
        run: python -m pytest

      - name: 既存のDiscord・Google Calendar設定を読み取り検証
        working-directory: app
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          GOOGLE_CALENDAR_ID: ${{ secrets.GOOGLE_CALENDAR_ID }}
        run: python scripts/validate_destinations.py

      - name: 状態保存ブランチを確認
        id: state-branch
        working-directory: .relay-repo
        shell: bash
        run: |
          if git ls-remote --exit-code --heads origin regional-cashless-state >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: 既存の状態を取得
        if: steps.state-branch.outputs.exists == 'true'
        uses: actions/checkout@v4
        with:
          ref: regional-cashless-state
          path: .relay-state
          fetch-depth: 1

      - name: 状態保存場所の元ファイルを取得
        if: steps.state-branch.outputs.exists == 'false'
        uses: actions/checkout@v4
        with:
          path: .relay-state
          fetch-depth: 1

      - name: 状態保存ブランチを初回作成
        if: steps.state-branch.outputs.exists == 'false'
        working-directory: .relay-state
        shell: bash
        run: |
          git checkout --orphan regional-cashless-state
          git rm -rf .
          mkdir -p state
          cp "$GITHUB_WORKSPACE/app/state/state.json" state/state.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state/state.json
          git commit -m "Initialize regional cashless state"
          git push origin regional-cashless-state

      - name: 4社の公式地域キャンペーンを監視
        working-directory: app
        shell: bash
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
          GOOGLE_CALENDAR_ID: ${{ secrets.GOOGLE_CALENDAR_ID }}
          MONITOR_USER_AGENT_CONTACT: https://github.com/nekoromme/regional-cashless-monitor
          STATE_FILE: ${{ github.workspace }}/.relay-state/state/state.json
          LOG_DIR: ${{ github.workspace }}/app/logs
        run: |
          set -o pipefail
          cashless-monitor --mode "$RUN_MODE" | tee monitor-summary.json

      - name: 状態を保存
        if: always()
        working-directory: .relay-state
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add state/state.json
          if git diff --cached --quiet; then
            echo "状態に変更はありません"
            exit 0
          fi
          git commit -m "Update regional cashless state ($GITHUB_RUN_ID)"
          git pull --rebase origin regional-cashless-state
          git push origin regional-cashless-state

      - name: 診断ログを保存
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regional-cashless-log-${{ github.run_id }}
          path: |
            app/logs/
            app/monitor-summary.json
          if-no-files-found: warn
          retention-days: 30
