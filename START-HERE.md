# はじめに読むファイル

このリポジトリは実装済みです。監視はGitHub Actionsから毎日6回、06:04、11:04、16:04、18:04、20:04、22:04（日本時間）に実行されます。

## 通常運用で行うこと

- 定期実行は自動です。普段の操作は不要です。
- 作品ごとの監視は、GitHubで`GAME_MONITOR_MODES.txt`の右側を`ON`/`OFF`にして切り替えます。
- 手動確認したい時は、GitHubの `Actions` → `monitor` → `Run workflow` → `run` を選びます。
- `baseline` と `arm` は初回設定専用です。すでに通知が動いている場合、やり直す必要はありません。
- DiscordまたはGoogle Calendarの通知先を変える時だけ、GitHub Secretsの値を更新します。

## 予定が登録される条件

- 対象5作品のBOX発売日は、未来365日以内の正確な発売日だけを終日予定で登録します。月しか公表されていない場合は予定を作りません。
- 店舗抽選は、今日以降の正確な受付開始日時だけを登録します。前日以前に終了した過去案件は、監視状態へ記録しますがCalendarへ遡って登録しません。
- Amazon招待販売のように開始日時が公表されない案件は、時刻を推測して登録しません。
- 同じゲーム・同じBOXは、公式ページと二次情報の両方で見つけても1件に統合します。公式情報を優先します。
- 旧バージョンがすでに作った重複予定は、安全のため自動削除しません。正しい方を残し、過去に作られた二次情報側の予定を1回だけ手動で削除してください。

## 店舗抽選の監視方法

- 公式ウェブで開始日時を取得できる場合は公式情報を優先します。
- メーカー公式販売元として、KONAMI STYLE、タカラトミーモール、ONE PIECEカードゲーム公式ショップのお知らせ、プレミアムバンダイのフュージョンワールドを監視します。遊戯王サテライトショップは監視しません。
- アプリ限定・ログイン依存・固定ページ上書き型の店舗は、SNKRDUNKの新弾別「予約・抽選情報まとめ」をフォールバックにします。
- ポケカ・ワンピカードの年間発売スケジュール記事から、新しい新弾別記事のURLを自動発見します。新弾のたびに `sites.yaml` へURLを手入力する必要はありません。
- 対象店舗はヤマダデンキ、コジマ、ゲオ、ヨドバシカメラ、ポケモンセンター、イオン系ウェブ応募、キッズリパブリック、Amazon、楽天ブックスです。

## GitHub Secrets

値はソースコードやチャットへ貼らず、GitHubリポジトリの `Settings` → `Secrets and variables` → `Actions` で管理します。

| Secret名 | 内容 |
|---|---|
| `DISCORD_WEBHOOK_URL` | 通知先DiscordチャンネルのWebhook URL |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | GoogleサービスアカウントのJSON全文 |
| `GOOGLE_CALENDAR_ID` | 監視専用Google CalendarのID |

User-Agentの連絡先にはworkflowが公開リポジトリURLを自動設定します。個人メールやGitHub Variableの追加は不要です。

## その他の文書

| ファイル | 用途 |
|---|---|
| `site-research.md` | 各社の調査結果と監視可否 |
| `GAME_MONITOR_MODES.txt` | 作品ごとの監視ON/OFF |
| `sites.yaml` | 実際にプログラムが読むサイト設定 |
| `implementation-spec.md` | 構成、重複防止、通知、Calendar、テストの仕様 |
| `source-index.md` | 調査した公式URLと二次情報URL |
| `unresolved-issues.md` | 公開情報だけでは断定できない事項 |
